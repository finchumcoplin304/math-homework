// Generate a random number between 1 and 10
$randomNumber = rand(1, 10);

// Use the random number to calculate a square root
$squareRoot = sqrt($randomNumber);

// Print the result
echo "The square root of $randomNumber is $squareRoot.";
